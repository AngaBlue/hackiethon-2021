// Returns username based off nextAuthID
function getUsername(nextAuthID: number): string {
    console.error("getUsername function is incomplete");
    return;
}

function getUserFriends(nextAuthID: number): Array<number> {
    console.error("getUserFriends function is incomplete");
    return;
}

function getUserIDByNextAuthAccessToken(nextAuthAccessToken: string): number {
    // This should find the appropriate access token from the "sessions" table and return the corresponding user_id
    console.error("getUserIDByNextAuthAccessToken function is incomplete");
    return;
}

export { getUsername, getUserFriends, getUserIDByNextAuthAccessToken };
